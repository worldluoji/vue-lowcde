import { install as installComponents } from './components';

import { install as installComponentPanels } from './panels';

export const install = {
  installComponents,
  installComponentPanels
};

export { CustomerComponents, CustomerComponentsInfo } from './components';
export { CustomerComponentPanels } from './panels';
