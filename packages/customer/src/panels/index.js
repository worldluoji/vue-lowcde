import SplitLinePanel from './SplitLine/SplitLinePanel.vue';

export const install = (Vue) => {
  Vue.component('SplitLinePanel', SplitLinePanel);
};

export const CustomerComponentPanels = {
  SplitLinePanel
};
